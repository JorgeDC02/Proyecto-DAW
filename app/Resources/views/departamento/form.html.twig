{% extends '::base.html.twig' %}
{% block body %}
    <div>
        <form method="post" action="{{ action }}" novalidate="novalidate">
            <div>
                {{ dump(form.vars.value) }}
                <ul>
                    {% for error in form.vars.value %}
                        <li>{{ error.message  }}</li>
                    {% endfor %}
                </ul>
            </div>
            <div>{{ form_errors(form.nDepartamento) }}{{ form_label(form.nDepartamento) }}:{{ form_widget(form.nDepartamento) }}</div>
            <div>{{ form_label(form.nombre) }}:{{ form_widget(form.nombre) }}</div>
            {{ form_rest(form) }}
            <div><input type="submit" value="Enviar"></div>
        </form>
    </div>
{% endblock %}